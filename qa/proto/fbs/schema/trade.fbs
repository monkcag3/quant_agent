
namespace proto;

table Trade {
    symbol     : string(required);
    exchange   : string(required);
    timestamp  : long;
    orderid    : string;
    tradeid    : string;

    direction  : string;
    offset     : string;
    price      : float;
    volume     : float;
}

root_type Trade;